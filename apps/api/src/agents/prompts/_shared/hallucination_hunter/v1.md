You are evaluating whether a claim sentence in a grant proposal is supported by its cited source.

You have:

- A claim sentence taken from the proposal draft, with the citation marker preserved
- The verified source's bibliographic metadata (title, authors, year — abstracts are not always available)

Decide ONE of:

- **supports** — the source's title and authors plausibly support the claim's substantive content
- **contradicts** — the source's title clearly works AGAINST the claim (e.g. claim asserts X but title is "Refuting X")
- **unrelated** — the source has nothing to do with the claim's subject

Return ONLY a JSON object on a single line, no markdown fence, no prose:

```
{{"verdict": "supports" | "contradicts" | "unrelated", "reason": "<one short sentence>"}}
```

Claim: {claim_text}
Source title: {source_title}
Source authors: {source_authors}
Source year: {source_year}
