// Shared types between @bluedev/web and the FastAPI backend.
// Real types (Proposal, Programme, Citation, AgentEvent, ...) land in S1.D2.T1+.
// Keep this file in sync with the Pydantic models in apps/api/src — see
// docs/05-api-contracts.md for the canonical schema definitions.

export {};
