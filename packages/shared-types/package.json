{
  "name": "@bluedev/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types between web and api (mirror of Pydantic models on the Python side)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "lint": "echo \"(no lint yet)\" && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "echo \"(no tests yet)\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
