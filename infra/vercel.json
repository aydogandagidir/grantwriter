{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @bluedev/web build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "echo 'install handled inside buildCommand for pnpm workspace correctness'",
  "ignoreCommand": "bash -lc 'git diff --quiet HEAD^ HEAD -- apps/web pnpm-lock.yaml package.json'",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}
